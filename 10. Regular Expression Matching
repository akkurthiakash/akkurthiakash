import re

def check_match(text, pattern):
    if re.match(pattern, text):
        return True
    else:
        return False

text = "Hello, World!"
pattern = r"Hello, \w+!"
result = check_match(text, pattern)
print(result)
